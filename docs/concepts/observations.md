# Observations

An observation is the canonical envelope emitted by a runtime adapter for one
case. It includes status, value, error, captured text, exit code, duration,
effects, artifacts and metadata.

